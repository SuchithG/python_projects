# Display the command line arguments

import sys

lst=sys.argv  # getting the list using sys.argv
for i in lst:print(i)

print(len(lst))  # Display the command line arguments length

print(lst[0]) # Display the script/Program name